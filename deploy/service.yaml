apiVersion: v1
kind: Service
metadata:
  name: exporter-service
  namespace: monitoring
spec:
  selector:
    app: good-exporter
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP 